;在屏幕显示代码，利用2号DOS指令
;利用串传送

DATA SEGMENT
STR DB '123ABC'
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
START:  MOV AX,DATA
        MOV DS,AX
	LEA BX,STR
	MOV CX,6
LPP:    MOV AH,2
	MOV DL,[BX]
	INC BX
	INT 21H
	LOOP LPP
	MOV AH,4CH
	INT 21H
CODE    ENDS
	END START